    private void newKeyboard(ArrayList<Word> k){
        switcher.setKeyboardList(keyboard);
        Random random = new Random(); //new
        int pos = 0; //nwe
        boolean available = false; //Check to see if the WOrd has already been used new
        ArrayList<Integer> used = new ArrayList<>(); //new
        
        int xCoord = 0;
        int yCoord = 0;
        int num=0;
        while(used.size() <= keyboard.size()) {
        //for (Word word : keyboard){
            while(available == false){
                pos = random.nextInt(keyboard.size());
                for(int place : used){
                    if(place != pos){
                        available = true;
                    }
                }
            }
            Word word = keyboard.get(pos);
            String name = "key"+num;
            word.newLabel(xCoord, yCoord, true, name);
            word.setLabelName(name);
            panelKeyboard.add(word.getLabel());
            xCoord+=50;
            num++;
            available = false;
        }
        
    }